<nav class="navbarMaqPro navbar navbar-expand-lg fixed-top navbar-dark">
    <div class="navContainer container-fluid">
        <a class="logoNavbarContainer navbar-brand mb-4" href="{{ path('home') }}"><span class="logoName">MaqPro</span></a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navItems navbar-nav">
                <a class="navlink nav-link" href="{{ path('makeup_index') }}">Produits beauté</a>
                <a class="navlink nav-link" href="">Effets spéciaux / Secourisme</a>
                <a class="navlink nav-link" href="">Grand publique</a>
                <a class="navlink nav-link" href="">Nos distributeurs</a>
                <a class="navlink nav-link" href="">Contactez nous !</a>

                {% if app.user %}
                    {% if is_granted("ROLE_ADMIN") %}
                        <a class="nav-link" href="{{ path('admin_index') }}">Administration</a>
                    {% endif %}
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
