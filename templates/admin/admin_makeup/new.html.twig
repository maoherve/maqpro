{% extends 'adminlayout.html.twig' %}

{% block main %}
    <div class="adminContainer">
        <h1>Create new Makeup</h1>

        {{ include('admin/admin_makeup/_form.html.twig') }}

        <a href="{{ path('admin_makeup_index') }}">back to list</a>
    </div>
{% endblock %}
