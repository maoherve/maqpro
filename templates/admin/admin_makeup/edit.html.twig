{% extends 'adminlayout.html.twig' %}

{% block main %}
    <div class="adminContainer">

        <h1>Edit Makeup</h1>

        {{ include('admin/admin_makeup/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('admin_makeup_index') }}">back to list</a>

        {{ include('admin/admin_makeup/_delete_form.html.twig') }}
    </div>
{% endblock %}
