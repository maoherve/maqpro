{% extends 'adminlayout.html.twig' %}

{% block main %}
    <div class="adminContainer">
        <h1>MakeupArtist index</h1>

        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Text</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for makeup_artist in makeup_artists %}
                <tr>
                    <td>{{ makeup_artist.id }}</td>
                    <td>{{ makeup_artist.text }}</td>
                    <td>
                        <a href="{{ path('admin_makeup_artist_edit', {'id': makeup_artist.id}) }}">edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('admin_makeup_artist_new') }}">Create new</a>
        <a href="{{ path('administration') }}">Back Menu</a>
    </div>
{% endblock %}
