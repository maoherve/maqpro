{% extends 'adminlayout.html.twig' %}

{% block main %}
    <div class="adminContainer">

        <h1>Edit MakeupRef</h1>

        {{ include('admin/makeup_ref/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('admin_makeup_ref_index') }}">back to list</a>

        {{ include('admin/makeup_ref/_delete_form.html.twig') }}
    </div>
{% endblock %}
