<!DOCTYPE html>

<title>Edit Colours</title>

{% block body %}
    <h1>Edit Colours</h1>

    {{ include('colours/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('colours_index') }}">back to list</a>

    {{ include('colours/_delete_form.html.twig') }}
{% endblock %}
