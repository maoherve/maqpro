<!DOCTYPE html>

<title>Edit Makeup</title>

{% block body %}
    <h1>Edit Makeup</h1>

    {{ include('admin_makeup/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_makeup_index') }}">back to list</a>

    {{ include('admin_makeup/_delete_form.html.twig') }}
{% endblock %}
