{% extends 'layout.html.twig' %}

{% block main %}
    <div class="productContainer container container-fluid mb-5">



        <div class="row">
            {% for makeup in makeups %}
                <div class="productCard container mt-5 ">

                    <div class="middle card-body">
                        <p class="productTitle card-text">{{ makeup.name }}</p>
                    </div>
                </div>
            {% else %}
                <div class="card-body"><p>Pas de produit disponible dans cette catégorie !</p></div>
            {% endfor %}
        </div>
    </div>
{% endblock %}